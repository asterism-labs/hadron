[package]
name = "hadron-vfs-protocol"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true
license.workspace = true
publish.workspace = true
description = "VFS wire protocol types shared between kernel and userspace"

[dependencies]
hadron-syscall = { path = "../hadron-syscall" }

[lints.rust]
unsafe_op_in_unsafe_fn = "warn"
missing_docs = "warn"

[lints.clippy]
all = "warn"
pedantic = "warn"
