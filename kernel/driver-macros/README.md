# hadron-driver-macros

Proc-macro crate for the Hadron driver framework. Provides the `#[hadron_driver(...)]` attribute macro that generates per-driver context types with compile-time capability enforcement, probe wrapper functions, and linker-section registration entries for PCI and platform drivers.
